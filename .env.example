# Example environment variables — placeholders only, DO NOT put real secrets here
# Copy this to .env locally and fill values.

# API / service
API_URL=https://api.example.com
API_KEY=REPLACE_WITH_YOUR_API_KEY

# Database (example)
DB_HOST=localhost
DB_PORT=5432
DB_USER=yourdbuser
DB_PASS=REPLACE_WITH_DB_PASSWORD

# Third-party service keys
STRIPE_KEY=REPLACE_WITH_STRIPE_KEY
SENDGRID_API_KEY=REPLACE_WITH_SENDGRID_KEY
