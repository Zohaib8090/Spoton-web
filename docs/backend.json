
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the HarmonyStream application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "username": {
          "type": "string",
          "description": "Username of the user."
        },
        "settings": {
          "type": "object",
          "description": "User-specific settings.",
          "properties": {
            "theme": {
              "type": "string",
              "enum": ["light", "dark", "system"],
              "description": "The user's preferred theme."
            },
            "notifications": {
              "type": "object",
              "properties": {
                "newReleases": { "type": "boolean" },
                "playlistUpdates": { "type": "boolean" }
              }
            },
            "playbackQuality": {
              "type": "object",
              "description": "The user's preferred playback quality for different connection types.",
              "properties": {
                "audio": {
                  "type": "object",
                  "properties": {
                    "wifi": {
                      "type": "string",
                      "enum": ["automatic", "low", "standard", "high", "very-high"],
                      "description": "Audio quality on Wi-Fi."
                    },
                    "cellular": {
                      "type": "string",
                      "enum": ["automatic", "low", "standard", "high", "very-high"],
                      "description": "Audio quality on cellular."
                    }
                  }
                },
                "video": {
                  "type": "object",
                  "properties": {
                    "wifi": {
                      "type": "string",
                      "enum": ["automatic", "low", "standard", "high", "very-high"],
                      "description": "Video quality on Wi-Fi."
                    },
                    "cellular": {
                      "type": "string",
                      "enum": ["automatic", "low", "standard", "high", "very-high"],
                      "description": "Video quality on cellular."
                    }
                  }
                }
              }
            },
            "streamingServices": {
              "type": "object",
              "description": "User's connected streaming services.",
              "properties": {
                "youtubeMusic": {
                  "type": "boolean",
                  "description": "Whether YouTube Music is enabled."
                }
              }
            },
            "trackTransitions": {
              "type": "object",
              "description": "User's track transition preferences.",
              "properties": {
                "gaplessPlayback": { "type": "boolean" },
                "automix": { "type": "boolean" },
                "crossfade": { "type": "number", "description": "Crossfade duration in seconds." }
              }
            },
            "listeningControls": {
              "type": "object",
              "description": "User's listening control preferences.",
              "properties": {
                "autoPlay": { "type": "boolean" },
                "monoAudio": { "type": "boolean" },
                "equaliserEnabled": { "type": "boolean" },
                "volumeNormalization": { "type": "boolean" },
                "balance": { "type": "number", "description": "Audio balance from -1 (left) to 1 (right)." }
              }
            },
             "equaliser": {
              "type": "array",
              "description": "10-band equaliser settings.",
              "items": { "type": "number" },
              "minItems": 10,
              "maxItems": 10
            }
          }
        }
      },
      "required": [
        "id",
        "email",
        "username"
      ]
    },
    "PinnedItem": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "PinnedItem",
        "type": "object",
        "description": "Represents a song, album, or playlist pinned by a user.",
        "properties": {
            "itemId": { "type": "string", "description": "The ID of the pinned item (song, playlist, or album)." },
            "type": { "type": "string", "enum": ["song", "playlist", "album"] },
            "name": { "type": "string" },
            "artist": { "type": "string", "description": "Artist or description of the item." },
            "albumArt": { "type": "string", "format": "uri" },
            "pinnedAt": { "type": "string", "format": "date-time" }
        },
        "required": ["itemId", "type", "name", "albumArt", "pinnedAt"]
    },
    "Playlist": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Playlist",
      "type": "object",
      "description": "Represents a playlist created by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Playlist entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to User. (Relationship: User 1:N Playlist)"
        },
        "name": {
          "type": "string",
          "description": "Name of the playlist."
        },
        "description": {
          "type": "string",
          "description": "Description of the playlist."
        },
        "trackIds": {
          "type": "array",
          "description": "References to Tracks. (Relationship: Playlist 1:N Track)",
          "items": {
            "type": "string"
          }
        },
        "coverArt": {
            "type": "string",
            "format": "uri"
        }
      },
      "required": [
        "id",
        "userId",
        "name"
      ]
    },
    "Track": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Track",
      "type": "object",
      "description": "Represents a music track.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Track entity."
        },
        "title": {
          "type": "string",
          "description": "Title of the track."
        },
        "artist": {
          "type": "string",
          "description": "Artist of the track."
        },
        "album": {
          "type": "string",
          "description": "Album the track belongs to."
        },
        "streamingService": {
          "type": "string",
          "description": "The streaming service the track is from (e.g., YouTube Music)."
        },
        "streamingServiceId": {
          "type.string": "The track's ID on the streaming service."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the track's album art.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "title",
        "artist",
        "streamingService",
        "streamingServiceId",
        "imageUrl"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. Uses path-based ownership for private user data.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/playlists/{playlistId}",
        "definition": {
          "entityName": "Playlist",
          "schema": {
            "$ref": "#/backend/entities/Playlist"
          },
          "description": "Stores playlists created by each user. Uses hierarchical paths to establish ownership. Supports secure list operations based on the 'userId' parameter.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            },
            {
              "name": "playlistId",
              "description": "The unique identifier of the playlist."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/pins/{pinId}",
        "definition": {
            "entityName": "PinnedItem",
            "schema": { "$ref": "#/backend/entities/PinnedItem" },
            "description": "Stores user's pinned items (songs, albums, playlists). Limited to 9 per user.",
            "params": [
                { "name": "userId", "description": "The unique identifier of the user." },
                { "name": "pinId", "description": "The unique identifier of the pin." }
            ]
        }
      },
      {
        "path": "/tracks/{trackId}",
        "definition": {
          "entityName": "Track",
          "schema": {
            "$ref": "#/backend/entities/Track"
          },
          "description": "Stores track metadata.  Tracks are global and not user-owned.",
          "params": [
            {
              "name": "trackId",
              "description": "The unique identifier of the track."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to manage users, playlists, and tracks, with a focus on scalability and security. User data is stored in a dedicated collection, while playlists are stored within each user's document using a subcollection. Tracks are stored in a global collection, since Tracks are not owned by users.\n\nThis design prioritizes **Authorization Independence**. Specifically, there is no `get()` call in security rules.\n\nThe design supports the required QAPs:\n\n*   **Secure Lists:**  Path-based ownership (`/users/{userId}/playlists/{playlistId}`) enables secure list operations for playlists, as rules can easily filter based on `request.auth.uid == userId`.\n\n*   **DBAC:**  Roles are derived from `request.auth.uid` and path-based ownership, precluding the need for custom claims.\n"
  }
}

    